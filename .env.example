# データベース（Supabase 等）
DATABASE_URL="postgresql://..."
DIRECT_URL="postgresql://..."

# NextAuth（必須）
AUTH_SECRET="32文字以上のランダムな文字列"
# 開発時は次のどちらか（ポートは npm run dev の -p に合わせる）
AUTH_URL="http://localhost:3002"
# NEXTAUTH_URL="http://localhost:3002"
